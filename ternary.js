//Ternary operation example:
// condition ? truthy_value : falsy_value
// condition is evaluated to true or false
// if condition is true, truthy_value is returned
// if condition is false, falsy_value is returned

// Example:
const age = 20;
let isAdult = age >= 18 ? 'yes' : 'no';
console.log(isAdult);


